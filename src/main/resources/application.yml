spring:
  datasource:
    url: jdbc:mysql://localhost:3306/travel_planner?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Base5Data! #actual!!!!!!!!
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false
  thymeleaf:
    cache: false
server:
  port: 8080